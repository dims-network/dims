# Dependencies for the analysis steps (RQA, cross-RQA, cross-wavelet).
#
# Notes on what is deliberately absent:
#   json  - part of the Python standard library; it is not a pip package, and
#           listing it made this whole file fail to install.
#   matplotlib - imported by nothing in this repo. pycwt depends on it, so it
#           still gets installed; it simply does not belong here as a direct
#           dependency.
#
# Floor pins rather than exact pins: these steps use long-stable APIs, and an
# exact pin here previously read "scipy==1.26.4", which does not exist (1.26.4
# is a numpy version).
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
pycwt==0.5.0b0
